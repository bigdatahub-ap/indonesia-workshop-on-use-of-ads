services:
  site:
    build: .
    restart: unless-stopped

  proxy:
    image: nginx:alpine
    volumes:
      - ./nginx/proxy.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - site
    restart: unless-stopped
